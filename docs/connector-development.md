# Connector Development

Connector authors should keep integrations explicit, testable, and separate from production customer environments. Public connector examples should use synthetic assets and caller-provided lab endpoints only.

Never embed credentials, internal endpoints, customer IP addresses, plant diagrams, or detection logic in a connector contribution.
